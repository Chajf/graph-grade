[project]
name = "graph-grade"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.3.2",
    "langchain-openrouter>=0.2.3",
    "langgraph>=1.2.2",
    "pydantic>=2.13.4",
    "python-dotenv>=1.0.1",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.15",
]
